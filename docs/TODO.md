# TODO（Daiza 実装タスク）

`docs/SPEC.md` の仕様に基づく残タスク一覧。完了した項目は削除している
（何をどう実装したかは `docs/SPEC.md` と git history を参照）。

## 1. 実機確認

実装と Node 上での検証は済んでいるが、ブラウザ／Illustrator の実機で目視確認できていないもの。

- [ ] 大画像：3000px 超の画像を読み込んでもフリーズしないこと（第1相解析は Web Worker 化済み）
- [ ] `.ai` エクスポート：Illustrator で開き、レイヤー・実寸・パス編集可否を確認する
- [ ] 3Dプレビュー：透明感・白版の見え方・傾け・分解／組立アニメーション・低スペック環境での動作
- [ ] 3Dの床：グリッドが原点から 10mm 刻みで並ぶこと、アクリル越しにも格子が見えること、
      アップロード画像が床へ反映されること
- [ ] 2Dグリッド：格子がルーラーの目盛りと一致すること、ズーム／パンに追従すること、
      3D へ切り替えて戻しても表示状態が保たれること

## 2. 解析解像度の上限（任意・既定OFF）

Web Worker 化してもなお重い超巨大画像向けの補助的対策。実機確認（§1）で問題が出たら着手する。

- [ ] 解析時のみ内部ダウンサンプリングを任意で行えるようにする
- [ ] その場合 `mm_per_pixel` は縮小後解像度で算出し、表示・SVG は原寸座標を用いる

## 3. 台座形状の拡張（角丸・円形・楕円・正多角形・任意形状）

SPEC「台座形状」「台座 footprint（形状の幾何）」節として策定済み・未実装。
矩形既定の現行挙動を厳密に維持したまま追加する（矩形選択時は従来と完全一致が要件）。

- [ ] model: `baseShape` と形状別パラメータ（角丸半径・台座直径・辺数・回転角）、
      台座形状ソースの状態、`baseShapeFailed` エラー種別、normalizeParameters
      （角丸半径クランプ・辺数の整数化）
- [ ] analysis/footprint（新規）: プリセット footprint 生成（矩形・角丸・円・楕円・正多角形）と
      正規化ソース → footprint のスケール。パス（ベジェ）＋折れ線（許容誤差 0.05mm）の二重表現
- [ ] analysis/base: 検査の一般化（スリット矩形の footprint 内包・重心投影の凸包内包）
- [ ] analysis/stability: 支持関数による 4 方向の一般化と最小転倒角（凸包の最近傍辺）・方位角
- [ ] 任意形状ソース読込: PNG シルエット（固定しきい値 α>127・最大パーツ・平滑化＋曲線補完）／
      SVG パス（getPointAtLength サンプリング）
- [ ] UI: 台座形状セレクタ・形状別パラメータ欄・形状ソースのファイル選択
- [ ] プレビュー: 前面図の一般化（bbox 幅）・上面図インセット（footprint／スリット／重心投影／
      最悪方位矢印）
- [ ] 3D: footprint の押し出し＋貫通スリット
- [ ] export: footprint の曲線パス出力（SVG / .ai 共通幾何）
- [ ] 結果表示: 台座形状・転倒角(最小・方位) の行を追加

## 将来拡張（バックログ）

- [ ] 複数差込口
- [ ] 台座 footprint の穴あき対応（ドーナツ形状等）
- [ ] 金属スタンド対応
- [ ] アクリル以外の素材
- [ ] 密度マップ
- [ ] パーツ分割
- [ ] 複数PNG同時計算
- [ ] PWA化 / WebAssembly置き換え
